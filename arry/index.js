//program to sum values of an array.

const arrySum = [1,2,3,4];
let zero=0;
arrySum.forEach(n=> zero += n)
console.log(zero);

//a program to remove a specific element from an array.

const arryValue = [
'apple',
'ball',
'cat'
]

const arrySplice = arryValue.slice(1,2);
console.log(arrySplice)

// a program to find the maximum and minimum value of an array

const arryMaxMin=[12,13,14,15,16];

const max = Math.max(...arryMaxMin)
const min = Math.min(...arryMaxMin)

console.log (max,min);

// a program to reverse an array of integer values.

const arryReverse = [1,2,3,4,5];

console.log(arryReverse.reverse());

// a program to find the second largest element in an array.

// let Num = [12,100,1000,1];

// const hello = (Num)=>{
// return Num.sort((a,b) return b-a)
// }
// console.log(hello) 

// a Java program to find the sum of the two elements of a given array equal to a given integer. Eg: Sample array: [1,2,4,5,6] Target value: 6.

const arry = [1,2,3,4,5,6]

const num =()=>{ arry.sort(a,b)}
console.log(num)


/* Write a function that converts an array of values from 
 miles to kilometres using the map method. In the end, add t
 he kilometres up in a new variable called 
 "totalDistanceInKilometers" and return this variable.
*/

const arrMiles = [

    { }
]






